#!/usr/bin/env tsx
/**
 * scripts/seed.ts
 *
 * Seeds a project + a few example tasks for local development. Stubbed until
 * M2 ships the Drizzle schema; SPEC §4 reserves this entry point.
 */

console.log('🎼 seed: not yet implemented (M2). See SPEC.md §6 for the schema.');
process.exit(0);
